query Apps($request: AppsRequest!) {
  apps(request: $request) {
    items {
      address
      metadata {
        name
        logo
        description
        url
      }
    }
    pageInfo {
      ...PaginatedResultInfo
    }
  }
}

